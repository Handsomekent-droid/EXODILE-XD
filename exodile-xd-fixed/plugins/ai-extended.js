'use strict';
const { fetchAI } = require('../lib/apiFallback');
const { getChannelInfo } = require('../lib/messageConfig');
const FOOTER = '\n✦ ᴅᴇᴠ ᴘʀɪᴍᴇ ᴋɪʟʟᴇʀ ɴᴏᴠᴀ ᴋᴇɴᴛ · ᴇxᴏᴅɪʟᴇ xᴅ';
function box(title, lines) { let t=`┏━━「 ${title} 」━━┓\n┃\n`;for(const l of lines)t+=`┃  ${l}\n`;return t+`┃\n┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛`+FOOTER; }
const SMART_FALLBACKS = ['Let me think on that... try again in a moment! 💭','Great question! My circuits are processing... retry! ⚡','I need a moment — try again shortly! 🤖','Almost got it — give me one more try! 🔄'];
function fallback(){ return SMART_FALLBACKS[Math.floor(Math.random()*SMART_FALLBACKS.length)]; }

module.exports = [
  {command:'explain',aliases:['explainit','breakdown'],category:'ai',description:'Explain any concept simply',usage:'.explain <concept>',
    async handler(sock,m,args,ctx={}){const ci=getChannelInfo();const chatId=ctx.chatId||m.key.remoteJid;const q=args.join(' ');if(!q)return sock.sendMessage(chatId,{text:box('🧠 *EXPLAIN*',['💀 Usage: .explain quantum physics']),...ci},{quoted:m});await sock.sendMessage(chatId,{react:{text:'🧠',key:m.key}});const ans=await fetchAI(`Explain "${q}" in simple terms anyone can understand. Be brief and clear.`)||fallback();await sock.sendMessage(chatId,{text:box('🧠 *EXPLANATION*',[`📚 Topic: ${q}`,``,ans]),...ci},{quoted:m});}},
  {command:'summarize',aliases:['tldr','summary'],category:'ai',description:'Summarize any text',usage:'.summarize <text>',
    async handler(sock,m,args,ctx={}){const ci=getChannelInfo();const chatId=ctx.chatId||m.key.remoteJid;const q=args.join(' ');if(!q)return sock.sendMessage(chatId,{text:box('📝 *SUMMARIZE*',['💀 Usage: .summarize <long text>']),...ci},{quoted:m});await sock.sendMessage(chatId,{react:{text:'📝',key:m.key}});const ans=await fetchAI(`Summarize this in 3-5 bullet points: "${q}"`)||fallback();await sock.sendMessage(chatId,{text:box('📝 *SUMMARY*',[ans]),...ci},{quoted:m});}},
  {command:'translate',aliases:['tr','translateto'],category:'ai',description:'Translate text to any language',usage:'.translate <lang> | <text>',
    async handler(sock,m,args,ctx={}){const ci=getChannelInfo();const chatId=ctx.chatId||m.key.remoteJid;const parts=args.join(' ').split('|');const lang=(parts[0]||'French').trim();const text=(parts[1]||'').trim();if(!text)return sock.sendMessage(chatId,{text:box('🌐 *TRANSLATE*',['💀 Usage: .translate French | Hello how are you']),...ci},{quoted:m});await sock.sendMessage(chatId,{react:{text:'🌐',key:m.key}});const ans=await fetchAI(`Translate this to ${lang}: "${text}". Reply with only the translation.`)||fallback();await sock.sendMessage(chatId,{text:box('🌐 *TRANSLATION*',[`🗣️ Language: ${lang}`,`📝 Original: ${text.slice(0,50)}`,``,`➤ ${ans}`]),...ci},{quoted:m});}},
  {command:'code',aliases:['codegen','writescode','coding'],category:'ai',description:'Generate code in any language',usage:'.code <language> | <task>',
    async handler(sock,m,args,ctx={}){const ci=getChannelInfo();const chatId=ctx.chatId||m.key.remoteJid;const parts=args.join(' ').split('|');const lang=(parts[0]||'JavaScript').trim();const task=(parts[1]||'').trim();if(!task)return sock.sendMessage(chatId,{text:box('💻 *CODE GEN*',['💀 Usage: .code Python | reverse a string']),...ci},{quoted:m});await sock.sendMessage(chatId,{react:{text:'💻',key:m.key}});const ans=await fetchAI(`Write a ${lang} function to: ${task}. Reply with only the code.`)||fallback();await sock.sendMessage(chatId,{text:`┏━━「 💻 *${lang.toUpperCase()} CODE* 」━━┓\n\`\`\`\n${ans}\n\`\`\`\n┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛`+FOOTER,...ci},{quoted:m});}},
  {command:'story',aliases:['writestory','makestory'],category:'ai',description:'AI generates a short story',usage:'.story <theme>',
    async handler(sock,m,args,ctx={}){const ci=getChannelInfo();const chatId=ctx.chatId||m.key.remoteJid;const theme=args.join(' ')||'a brave warrior in a dark world';await sock.sendMessage(chatId,{react:{text:'📖',key:m.key}});const ans=await fetchAI(`Write a short exciting story (150 words) about: ${theme}`)||fallback();await sock.sendMessage(chatId,{text:box('📖 *AI STORY*',[ans]),...ci},{quoted:m});}},
  {command:'poem',aliases:['writepoem','poetry'],category:'ai',description:'AI writes a poem',usage:'.poem <topic>',
    async handler(sock,m,args,ctx={}){const ci=getChannelInfo();const chatId=ctx.chatId||m.key.remoteJid;const topic=args.join(' ')||'life and darkness';await sock.sendMessage(chatId,{react:{text:'🎭',key:m.key}});const ans=await fetchAI(`Write a short creative poem about: ${topic}. Make it atmospheric and emotional.`)||fallback();await sock.sendMessage(chatId,{text:box('🎭 *AI POEM*',[ans]),...ci},{quoted:m});}},
  {command:'roastme',aliases:['aiburn','airoast'],category:'ai',description:'AI generates a roast for you',usage:'.roastme',
    async handler(sock,m,args,ctx={}){const ci=getChannelInfo();const chatId=ctx.chatId||m.key.remoteJid;await sock.sendMessage(chatId,{react:{text:'🔥',key:m.key}});const ans=await fetchAI('Write a funny, witty roast for someone who just asked a bot to roast them. Keep it light and humorous.')||'You asked a robot to roast you. That itself is the roast. 💀';await sock.sendMessage(chatId,{text:box('🔥 *AI ROAST*',[ans]),...ci},{quoted:m});}},
  {command:'nameanalysis',aliases:['myname','analyzename'],category:'ai',description:'AI analyzes your name meaning',usage:'.nameanalysis <name>',
    async handler(sock,m,args,ctx={}){const ci=getChannelInfo();const chatId=ctx.chatId||m.key.remoteJid;const name=args.join(' ');if(!name)return sock.sendMessage(chatId,{text:box('🔤 *NAME ANALYSIS*',['💀 Usage: .nameanalysis <your name>']),...ci},{quoted:m});await sock.sendMessage(chatId,{react:{text:'🔤',key:m.key}});const ans=await fetchAI(`Analyze the name "${name}": its origin, meaning, personality traits associated with it, and a fun fact. Be brief.`)||fallback();await sock.sendMessage(chatId,{text:box('🔤 *NAME ANALYSIS*',[`👤 Name: *${name}*`,``,ans]),...ci},{quoted:m});}},
  {command:'debug',aliases:['fixcode','bugfix'],category:'ai',description:'AI debugs your code',usage:'.debug <code with bug>',
    async handler(sock,m,args,ctx={}){const ci=getChannelInfo();const chatId=ctx.chatId||m.key.remoteJid;const code=args.join(' ');if(!code)return sock.sendMessage(chatId,{text:box('🐛 *DEBUG*',['💀 Usage: .debug <paste your code here>']),...ci},{quoted:m});await sock.sendMessage(chatId,{react:{text:'🐛',key:m.key}});const ans=await fetchAI(`Find and fix bugs in this code, explain what was wrong: ${code}`)||fallback();await sock.sendMessage(chatId,{text:box('🐛 *CODE DEBUG*',[ans]),...ci},{quoted:m});}},
  {command:'career',aliases:['jobadvice','careeradvice'],category:'ai',description:'AI career advice',usage:'.career <your field or question>',
    async handler(sock,m,args,ctx={}){const ci=getChannelInfo();const chatId=ctx.chatId||m.key.remoteJid;const q=args.join(' ')||'tech career';await sock.sendMessage(chatId,{react:{text:'💼',key:m.key}});const ans=await fetchAI(`Give practical career advice for someone in ${q}. Include actionable tips.`)||fallback();await sock.sendMessage(chatId,{text:box('💼 *CAREER ADVICE*',[ans]),...ci},{quoted:m});}},
  {command:'fitness',aliases:['workout','exercise','fitadvice'],category:'ai',description:'AI fitness plan',usage:'.fitness <goal>',
    async handler(sock,m,args,ctx={}){const ci=getChannelInfo();const chatId=ctx.chatId||m.key.remoteJid;const goal=args.join(' ')||'lose weight';await sock.sendMessage(chatId,{react:{text:'💪',key:m.key}});const ans=await fetchAI(`Create a simple weekly fitness plan for someone who wants to: ${goal}. Be practical and realistic.`)||fallback();await sock.sendMessage(chatId,{text:box('💪 *FITNESS PLAN*',[ans]),...ci},{quoted:m});}},
  {command:'recipe2',aliases:['cookingai','aicook'],category:'ai',description:'AI creates a recipe from ingredients',usage:'.recipe2 <ingredients>',
    async handler(sock,m,args,ctx={}){const ci=getChannelInfo();const chatId=ctx.chatId||m.key.remoteJid;const ing=args.join(' ');if(!ing)return sock.sendMessage(chatId,{text:box('🍳 *AI RECIPE*',['💀 Usage: .recipe2 chicken, rice, tomatoes']),...ci},{quoted:m});await sock.sendMessage(chatId,{react:{text:'🍳',key:m.key}});const ans=await fetchAI(`Create a recipe using these ingredients: ${ing}. Include steps.`)||fallback();await sock.sendMessage(chatId,{text:box('🍳 *AI RECIPE*',[ans]),...ci},{quoted:m});}},
  {command:'mindset',aliases:['psychology','mentalhealth'],category:'ai',description:'AI mindset and mental wellness advice',usage:'.mindset <situation>',
    async handler(sock,m,args,ctx={}){const ci=getChannelInfo();const chatId=ctx.chatId||m.key.remoteJid;const situation=args.join(' ')||'feeling stressed and unmotivated';await sock.sendMessage(chatId,{react:{text:'🧘',key:m.key}});const ans=await fetchAI(`Give supportive, practical mindset advice for someone who is: ${situation}. Be empathetic and helpful.`)||fallback();await sock.sendMessage(chatId,{text:box('🧘 *MINDSET COACH*',[ans]),...ci},{quoted:m});}},
  {command:'debate',aliases:['arguefor','makeargument'],category:'ai',description:'AI argues a side of a debate',usage:'.debate <topic> | <side>',
    async handler(sock,m,args,ctx={}){const ci=getChannelInfo();const chatId=ctx.chatId||m.key.remoteJid;const parts=args.join(' ').split('|');const topic=(parts[0]||'AI will replace humans').trim();const side=(parts[1]||'for').trim();await sock.sendMessage(chatId,{react:{text:'⚔️',key:m.key}});const ans=await fetchAI(`Make a strong argument ${side} this topic: "${topic}". Be persuasive and concise.`)||fallback();await sock.sendMessage(chatId,{text:box('⚔️ *AI DEBATE*',[`📋 Topic: ${topic}`,`🎯 Side: ${side}`,``,ans]),...ci},{quoted:m});}},
  {command:'rewrite',aliases:['rephrase','rewordtext'],category:'ai',description:'AI rewrites text in a new style',usage:'.rewrite <formal/funny/professional> | <text>',
    async handler(sock,m,args,ctx={}){const ci=getChannelInfo();const chatId=ctx.chatId||m.key.remoteJid;const parts=args.join(' ').split('|');const style=(parts[0]||'professional').trim();const text=(parts[1]||'').trim();if(!text)return sock.sendMessage(chatId,{text:box('✍️ *REWRITE*',['💀 Usage: .rewrite funny | I need a day off work']),...ci},{quoted:m});await sock.sendMessage(chatId,{react:{text:'✍️',key:m.key}});const ans=await fetchAI(`Rewrite this in a ${style} style: "${text}". Reply with only the rewritten version.`)||fallback();await sock.sendMessage(chatId,{text:box('✍️ *REWRITTEN*',[`🎨 Style: ${style}`,``,ans]),...ci},{quoted:m});}},
];
